#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../../.." && pwd)"
export FREECIV_PLAYER_NAME="AgentC"
cd "${REPO_ROOT}"
exec python3 -m freeciv_agent.player_cli "$@"
